# Ignore node_modules and npm's package-lock.json (if using npm)
node_modules/
npm-debug.log
package-lock.json

# Ignore Yarn's lock file (if using Yarn)
yarn.lock

# Ignore any build artifacts
/dist/

# Ignore environment files
.env.local
.env.*.local
.env.*.production

# Ignore logs and debugging files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore VSCode settings (optional, if using Visual Studio Code)
.vscode/
.idea/

# Ignore macOS system files (optional, if developing on macOS)
.DS_Store

# Ignore .env files with sensitive data (optional, only if required)
.env
