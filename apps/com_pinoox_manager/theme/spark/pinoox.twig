const PINOOX = {
    URL: {
        CURRENT: window.location.href,
        APP: '{{ url().app }}',
        BASE: '{{ url().appPath }}',
        API: '{{ url().api }}',
        SITE: '{{ url().site }}',
        DOMAIN: '{{ url().domain }}',
        PATH: '{{ url().path }}',
        THEME: '{{ assets() }}',
        RES: '{{ url().resource("resources/") }}',
        AVATAR: '{{ url().resource("resources/avatar.png") }}',
        APP_ICON: '{{ url().resource("resources/default.png") }}',
    },
};