# PHP backend origin — used first to build __PINOOX__.url (APP, API, SITE, …)
VITE_SERVER_URL=http://localhost/manager

# Optional overrides (usually auto-derived from VITE_SERVER_URL + VITE_APP_PATH)
VITE_API_PATH=/api/v1/
